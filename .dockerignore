.env
*.db
# Re-include the canonical content DB — baked as /app/seed/cravings.db
# and UPSERTed into the volume DB at startup by db.seed_sync.
!cravings.db
.git
.claude
.scratch
frontend/node_modules
frontend/dist
__pycache__
**/__pycache__
*.py[oc]
.venv
.pytest_cache
docs
tests
*.zip
# Prod reads food images from the rsync'd volume mount, not the image (see
# project_vps_image_state) — the ~89MB dir was busting the COPY layer on
# every source edit for nothing. mkdir -p in the Dockerfile makes the mount point.
images
# TWA project (Bubblewrap keystore/build output) — irrelevant to the server image, COPY . . would otherwise pull it in
twa
