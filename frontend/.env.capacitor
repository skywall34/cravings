# Loaded only by the Capacitor build (vite build --mode capacitor). Points the
# bundled app at the live prod API. The web build does NOT load this file, so the
# web app stays same-origin (BASE_URL=/cravings/).
VITE_API_BASE_URL=https://themshin.com/cravings
